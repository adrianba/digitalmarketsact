worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /tmp/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;
    tcp_nopush on;
    keepalive_timeout 65;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 256;
    gzip_types
        text/html
        text/css
        text/javascript
        application/javascript
        application/json
        image/svg+xml;

    # Redirect dma.bz → digitalmarketsact.com (301)
    server {
        listen 80;
        server_name dma.bz www.dma.bz;
        return 301 https://digitalmarketsact.com$request_uri;
    }

    # Redirect www → non-www (301)
    server {
        listen 80;
        server_name ~^www\.(.+)$;
        return 301 $scheme://$1$request_uri;
    }

    server {
        listen 80 default_server;
        server_name digitalmarketsact.com;
        root /usr/share/nginx/html;
        index index.html;

        # Security headers
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        # Cache static assets (CSS, JS, JSON) for 1 year
        location ~* \.(css|js|json|svg|png|ico|woff2?)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # HTML pages — no cache so updates are immediate
        location ~* \.html$ {
            expires -1;
            add_header Cache-Control "no-cache";
        }

        # Clean URLs: try file, then directory index, then 404
        location / {
            try_files $uri $uri/index.html $uri.html =404;
        }

        # Custom 404
        error_page 404 /404.html;
    }
}
